---
phase: 02-stripe-connect-wallet-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lib/src/stripe.ts
  - packages/lib/index.ts
  - apps/backend/.env
  - apps/backend/.env.example
autonomous: true
requirements:
  - STC-02
  - STC-03
  - STC-04
must_haves:
  truths:
    - Stripe singleton importable from @workspace/lib in apps/backend
    - Missing STRIPE_SECRET_KEY throws Error STRIPE_SECRET_KEY is not set at module load
    - apps/backend/.env has STRIPE_SECRET_KEY and DASHBOARD_URL
  artifacts:
    - path: packages/lib/src/stripe.ts
      provides: Stripe singleton
      exports: [stripe]
    - path: packages/lib/index.ts
      provides: Re-export stripe
      contains: export { stripe } from
    - path: apps/backend/.env
      provides: Runtime env vars
      contains: STRIPE_SECRET_KEY
  key_links:
    - from: apps/backend/src/routes/stripe.ts (Plan 02)
      to: packages/lib/src/stripe.ts
      via: import { stripe } from @workspace/lib
      pattern: stripe.*from.*@workspace/lib
---

## Phase Goal

**As a** seller, **I want to** connect my Stripe account during onboarding, **so that** I can receive payouts for digital product sales.

<objective>
Install Stripe SDK and create the stripe singleton in @workspace/lib so Plan 02 handlers can import it.
Add STRIPE_SECRET_KEY and DASHBOARD_URL to apps/backend/.env and .env.example.

Purpose: One authenticated Stripe client shared across apps, mirroring @workspace/db pattern.
Output: packages/lib/src/stripe.ts, updated packages/lib/index.ts, updated .env files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-stripe-connect-wallet-infrastructure/02-CONTEXT.md
@.planning/phases/02-stripe-connect-wallet-infrastructure/02-RESEARCH.md

<interfaces>
packages/lib/index.ts current state (verified by planner):
  export * from './actions/actions';
  export { formatPrice } from './src/utils/formatPrice';
Append new stripe export -- do NOT remove these lines.

Singleton guard pattern (mirror apps/backend/src/lib/sender.ts):
- Guard env var at module top, throw descriptive Error if missing
- Export initialized client directly, not via getter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Stripe SDK in @workspace/lib and backend</name>
  <files>packages/lib/package.json, apps/backend/package.json, pnpm-lock.yaml</files>
  <read_first>
    - packages/lib/package.json -- confirm stripe absent before installing
    - apps/backend/package.json -- confirm stripe absent before installing
  </read_first>
  <action>
    Run both from monorepo root. Both filters required -- pnpm hoisting may cause runtime module-not-found if stripe is only in one package (research A3):

      pnpm add stripe --filter @workspace/lib
      pnpm add stripe --filter backend

    Do not add any other packages.
  </action>
  <verify>
    <automated>grep stripe packages/lib/package.json && grep stripe apps/backend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - stripe in packages/lib/package.json dependencies, version 22.x
    - stripe in apps/backend/package.json dependencies, version 22.x
    - pnpm exits code 0, no peer-dependency errors
  </acceptance_criteria>
  <done>Both package.json files list stripe; lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 2: Create Stripe singleton and wire env vars</name>
  <files>packages/lib/src/stripe.ts, packages/lib/index.ts, apps/backend/.env, apps/backend/.env.example</files>
  <read_first>
    - packages/lib/index.ts -- read all lines; preserve every existing export
    - apps/backend/.env -- append only entries not already present
    - apps/backend/.env.example -- append only entries not already present
    - apps/backend/src/lib/sender.ts -- singleton guard pattern reference
  </read_first>
  <action>
    1. CREATE packages/lib/src/stripe.ts with these three lines:
         import Stripe from 'stripe';
         if (!process.env.STRIPE_SECRET_KEY) throw new Error('STRIPE_SECRET_KEY is not set');
         export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY);
       No apiVersion override (SDK bundled default, research A4).

    2. APPEND to packages/lib/index.ts (keep existing two lines):
         export { stripe } from './src/stripe';

    3. APPEND to apps/backend/.env if not already present:
         STRIPE_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_KEY
         DASHBOARD_URL=http://localhost:3001

    4. APPEND same two lines to apps/backend/.env.example.
  </action>
  <verify>
    <automated>grep 'export { stripe }' packages/lib/index.ts && grep STRIPE_SECRET_KEY apps/backend/.env && grep DASHBOARD_URL apps/backend/.env</automated>
  </verify>
  <acceptance_criteria>
    - packages/lib/src/stripe.ts contains: throw new Error('STRIPE_SECRET_KEY is not set')
    - packages/lib/src/stripe.ts contains: export const stripe = new Stripe(
    - packages/lib/index.ts contains: export { stripe } from './src/stripe'
    - packages/lib/index.ts still has: export * from './actions/actions'
    - packages/lib/index.ts still has: export { formatPrice }
    - apps/backend/.env line: STRIPE_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_KEY
    - apps/backend/.env line: DASHBOARD_URL=http://localhost:3001
    - apps/backend/.env.example has STRIPE_SECRET_KEY= and DASHBOARD_URL=
    - pnpm --filter @workspace/lib tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Stripe singleton importable from @workspace/lib; env vars in both .env files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env -> process | STRIPE_SECRET_KEY from .env at process start; never forwarded to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-01 | Information Disclosure | packages/lib/src/stripe.ts | mitigate | Key from env only; never serialised or logged; .env in .gitignore |
| T-02-02 | Tampering | apps/backend/.env.example | accept | Holds placeholder only; no real secret committed |
</threat_model>

<verification>
1. grep stripe packages/lib/package.json -- version 22.x
2. grep stripe apps/backend/package.json -- version 22.x
3. grep 'export { stripe }' packages/lib/index.ts -- export line present
4. grep STRIPE_SECRET_KEY apps/backend/.env -- env line present
5. grep DASHBOARD_URL apps/backend/.env -- env line present
6. pnpm --filter @workspace/lib tsc --noEmit -- exits 0
</verification>

<success_criteria>
- import { stripe } from '@workspace/lib' resolves without TypeScript error
- Missing STRIPE_SECRET_KEY throws 'STRIPE_SECRET_KEY is not set' at module load
- STRIPE_SECRET_KEY and DASHBOARD_URL in apps/backend/.env and .env.example
- Existing exports in packages/lib/index.ts unchanged
</success_criteria>

<output>
After completion, create .planning/phases/02-stripe-connect-wallet-infrastructure/02-01-SUMMARY.md using the summary template.
</output>
